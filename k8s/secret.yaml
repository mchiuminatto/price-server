apiVersion: v1
kind: Secret
metadata:
  name: price-server-secret
  namespace: price-server
type: Opaque
stringData:
  # Replace with actual credentials via CI/CD or external secrets manager (e.g., AWS Secrets Manager)
  DATABASE_URL: "postgresql+asyncpg://<DB_USER>:<DB_PASSWORD>@<DB_HOST>:5432/priceserver"
